import React from 'react';
import Team from '../../Team';

const TeamGastricBandits = () => {
  return (
    <Team teamName={'GastricBandits'}>
      {/* Add the specific content for the "GastricBandits" team here */}
      <h2>GastricBandits Team Page</h2>
      <p>Additional information about the GastricBandits team.</p>
    </Team>
  );
};

export default TeamGastricBandits;
